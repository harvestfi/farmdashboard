import {HarvestFilterPipe} from './harvest-filter.pipe';

describe('HarvestFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HarvestFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
