import {UniswapFilterPipe} from './uniswap-filter.pipe';

describe('UniswapFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UniswapFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
