import {HardworkFilterPipe} from './hardwork-filter.pipe';

describe('HardworkFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HardworkFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
